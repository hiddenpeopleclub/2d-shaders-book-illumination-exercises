using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpriteLight : MonoBehaviour 
{
    public enum LightTypes
    {
        Ambient,
        Directional,
        Point
    };

    // Light properties (Colors, Direction, LightType, etc)
    // ...
}
